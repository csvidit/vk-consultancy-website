const WhyUsCardContainer = (props: {children: React.ReactNode}) => {
    return(<div className="flex flex-col justify-center min-h-screen w-full">{props.children}</div>)
};

export default WhyUsCardContainer;